pluginManagement {
    repositories {
        gradlePluginPortal()
        maven { url = 'https://maven.neoforged.net/releases' }
    }
}
rootProject.name = 'driftward-climate'
include 'core'   // D14: the MC-less simulation engine (pure Kotlin/JVM)
